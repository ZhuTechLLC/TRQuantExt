# 韬睿量化专业版 - 系统总览

## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台







## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台





## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台


## 📌 系统架构

```
韬睿量化专业版
├── 🖥️ GUI应用 (PyQt6)
│   ├── 首页概览
│   ├── 投研分析（多因子分析）
│   ├── 策略开发（AI辅助）
│   ├── 回测验证
│   ├── 实盘交易（PTrade/QMT）
│   └── 系统设置
│
├── 🌐 Dashboard (Flask Web)
│   ├── 策略管理（多平台）
│   ├── 回测报告
│   ├── 研究文档
│   ├── 因子库
│   └── 交易记录
│
├── 🔗 Bridge服务 (FastAPI)
│   ├── PTrade Bridge (:8001)
│   ├── QMT Bridge (:8002)
│   └── QuantConnect Bridge (:8003)
│
└── 📊 数据中台
    ├── JQData (聚宽)
    ├── TuShare
    ├── Wind
    └── 本地CSV
```

## 🚀 快速启动

### 方式1: 桌面快捷方式
- 在应用菜单中搜索 "韬睿量化"
- 或点击桌面上的 jqquant.desktop

### 方式2: 终端命令
```bash
jqquant
```

### 方式3: Docker部署
```bash
# 启动GUI
./scripts/docker_manager.sh gui

# 启动后台服务
./scripts/docker_manager.sh services

# 查看状态
./scripts/docker_manager.sh status
```

## 📁 目录结构

```
JQQuant/
├── gui/                    # PyQt6 GUI模块
│   ├── main_window.py      # 主窗口
│   ├── widgets/            # 各功能面板
│   ├── styles/             # 主题样式
│   └── resources/          # 图标资源
│
├── dashboard/              # Web仪表盘
│   ├── dashboard_server.py # Flask服务
│   └── templates/          # HTML模板
│
├── core/                   # 核心模块
│   ├── data_center.py      # 数据中台
│   ├── backtest_engine.py  # 回测引擎
│   ├── strategy_manager.py # 策略管理
│   └── broker/             # 券商适配器
│
├── ptrade_bridge/          # PTrade桥接服务
├── qmt_bridge/             # QMT桥接服务
├── quantconnect_bridge/    # QuantConnect桥接服务
│
├── strategies/             # 策略文件
│   ├── examples/           # 本地示例
│   ├── ptrade/             # PTrade策略
│   ├── qmt/                # QMT策略
│   └── quantconnect/       # QuantConnect策略
│
├── data/                   # 数据目录
│   ├── ptrade/             # PTrade数据
│   ├── qmt/                # QMT数据
│   └── quantconnect/       # QuantConnect数据
│
├── docs/                   # 文档
├── prompts/                # AI提示词模板
├── scripts/                # 管理脚本
└── config/                 # 配置文件
```

## 🔧 管理脚本

### 系统安装与配置
```bash
# 安装/更新
./scripts/setup_system.sh install

# 查看状态
./scripts/setup_system.sh status

# 仅更新快捷方式
./scripts/setup_system.sh update

# 卸载
./scripts/setup_system.sh uninstall
```

### Docker管理
```bash
# 构建镜像
./scripts/docker_manager.sh build

# 启动GUI
./scripts/docker_manager.sh gui

# 启动所有服务
./scripts/docker_manager.sh services

# 停止服务
./scripts/docker_manager.sh stop

# 查看日志
./scripts/docker_manager.sh logs [service]
```

### Git自动提交
```bash
# 立即提交
./scripts/auto_git_commit.sh commit

# 守护模式（持续监控）
./scripts/auto_git_commit.sh daemon

# 自定义配置
GIT_WATCH_INTERVAL=60 GIT_AUTO_PUSH=true ./scripts/auto_git_commit.sh daemon
```

## 🔌 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Dashboard | 5000 | Web文件管理系统 |
| PTrade Bridge | 8001 | PTrade数据桥接 |
| QMT Bridge | 8002 | QMT数据桥接 |
| QuantConnect Bridge | 8003 | QuantConnect数据桥接 |

## 📊 量化工作流程

```
1. 信息获取
   └── JQData/TuShare/Wind → 数据中台 → 本地缓存

2. 因子构建
   └── 投研分析面板 → 多因子分析 → 因子评分

3. 策略开发
   └── Cursor AI辅助 → PTrade/QMT策略代码生成

4. 回测验证
   └── 本地回测 / PTrade回测 / QMT回测 → 报告分析

5. 策略迭代
   └── 回测结果 → AI分析 → 参数优化 → 重新回测

6. 实盘交易
   └── PTrade/QMT → 风控检查 → 自动执行
```

## 🗂️ 版本管理

- **安装目录**: `~/.local/share/jqquant`
- **归档目录**: `~/.local/share/jqquant_archive`
- **启动器**: `~/.local/bin/jqquant`
- **快捷方式**: `~/.local/share/applications/jqquant.desktop`

每次安装会自动归档旧版本，保留最近3个归档。

## 🔐 配置文件

- `config/jqdata_config.json` - JQData账号配置
- `config/broker_config.json` - 券商配置
- `config/ptrade_bridge_config.json` - PTrade桥接配置

## 📝 日志文件

- `logs/jqquant.log` - 主应用日志
- Docker日志: `./scripts/docker_manager.sh logs`

---

**韬睿量化专业版 v2.0**  
机构级量化研究与交易平台














