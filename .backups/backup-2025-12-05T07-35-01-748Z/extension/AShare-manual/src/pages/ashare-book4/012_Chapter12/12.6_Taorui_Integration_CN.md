---
title: "12.6 韬睿平台数据接入"
description: "学习在韬睿量化平台中接入事件和另类数据"
lang: "zh-CN"
layout: "/src/layouts/Layout.astro"
currentBook: "ashare-quantitative"
currentChapter: "ashare-quantitative-chapter12"
updateDate: "2025-01-26"
---

# 🔌 12.6 韬睿平台数据接入

> **核心摘要：**
> 
> 本节介绍如何在韬睿量化平台中接入事件数据和另类数据，构建完整的事件驱动策略。

## 📊 韬睿平台数据架构

<div class="exchange-grid">
  <div class="exchange-card">
    <div class="exchange-header">
      <span class="exchange-icon">📊</span>
      <h3>JQData数据</h3>
    </div>
    <div class="exchange-content">
      <p><strong>行情数据：</strong>日K、分钟K、Tick</p>
      <p><strong>财务数据：</strong>三大报表、财务指标</p>
      <p><strong>事件数据：</strong>业绩预告、增减持</p>
      <p><strong>因子数据：</strong>预计算因子库</p>
    </div>
  </div>

  <div class="exchange-card">
    <div class="exchange-header">
      <span class="exchange-icon">📰</span>
      <h3>另类数据</h3>
    </div>
    <div class="exchange-content">
      <p><strong>舆情数据：</strong>第三方API接入</p>
      <p><strong>分析师数据：</strong>Wind/iFinD</p>
      <p><strong>行业数据：</strong>行业协会、统计局</p>
      <p><strong>自定义数据：</strong>CSV/数据库导入</p>
    </div>
  </div>
</div>

## 💻 数据接入代码

<div class="board-details">
  <div class="board-card">
    <h4>📊 韬睿平台数据接入示例</h4>
    <div class="board-content">

```python
# 韬睿量化平台数据接入
from jqdatasdk import *
import pandas as pd

class TaoruiDataManager:
    """韬睿数据管理器"""
    
    def __init__(self):
        # 初始化JQData连接
        auth('your_account', 'your_password')
    
    def get_earnings_forecast(self, stocks, date):
        """获取业绩预告数据"""
        return get_STK_FIN_FORCAST(
            code=stocks,
            start_date=date
        )
    
    def get_index_change(self, index_code):
        """获取指数成分股调整"""
        return get_index_stocks_change(index_code)
    
    def get_holder_change(self, stocks, date):
        """获取增减持数据"""
        return get_STK_HOLDER_NUM(
            code=stocks,
            start_date=date
        )
    
    def import_custom_data(self, file_path):
        """导入自定义数据"""
        if file_path.endswith('.csv'):
            return pd.read_csv(file_path)
        elif file_path.endswith('.parquet'):
            return pd.read_parquet(file_path)
    
    def get_sentiment_data(self, stock, date):
        """获取舆情数据（需接入第三方API）"""
        # 示例：接入萝卜投研API
        # api_url = f"https://api.luobotouyan.com/sentiment/{stock}"
        # response = requests.get(api_url)
        # return response.json()
        pass

# 使用示例
dm = TaoruiDataManager()
forecast = dm.get_earnings_forecast(['000001.XSHE'], '2024-01-01')
```

</div>
  </div>
</div>

## 📈 事件驱动策略模板

<div class="board-details">
  <div class="board-card">
    <h4>📊 事件驱动策略模板</h4>
    <div class="board-content">

```python
# 韬睿量化平台事件驱动策略
class EventDrivenStrategy:
    """事件驱动策略"""
    
    def __init__(self):
        self.data_manager = TaoruiDataManager()
    
    def get_signals(self, date):
        """生成交易信号"""
        signals = []
        
        # 1. 业绩超预期信号
        forecast = self.data_manager.get_earnings_forecast(
            get_all_securities().index.tolist(), 
            date
        )
        positive_forecast = forecast[forecast['change_min'] > 30]
        signals.extend(positive_forecast['code'].tolist())
        
        # 2. 指数调入信号
        index_change = self.data_manager.get_index_change('000300.XSHG')
        signals.extend(index_change['add_stocks'])
        
        # 3. 大股东增持信号
        holder_change = self.data_manager.get_holder_change(
            get_all_securities().index.tolist(),
            date
        )
        increase = holder_change[holder_change['change'] > 0.01]
        signals.extend(increase['code'].tolist())
        
        return list(set(signals))
```

</div>
  </div>
</div>

## 🔮 总结与展望

<div class="summary-outlook">
  <h3>本章回顾</h3>
  <p>本章系统介绍了事件驱动策略和另类数据在A股量化投资中的应用，包括财报事件、新闻舆情、分析师预期等数据的获取和量化方法。</p>
  
  <h3>下章预告</h3>
  <p>下一章将全面介绍韬睿量化系统的架构和使用方法。</p>
  
  <a href="/ashare-book4/013_Chapter13_Taorui_Integration_CN" class="next-section">
    继续学习：第十三章 韬睿量化系统集成 →
  </a>
</div>













