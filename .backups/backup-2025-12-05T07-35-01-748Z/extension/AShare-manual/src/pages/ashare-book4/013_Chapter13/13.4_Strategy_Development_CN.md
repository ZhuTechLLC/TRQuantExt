---
title: "13.4 策略开发模块"
description: "掌握韬睿系统的策略开发流程、模板和最佳实践"
lang: "zh-CN"
layout: "/src/layouts/Layout.astro"
currentBook: "ashare-quantitative"
currentChapter: "ashare-quantitative-chapter13"
updateDate: "2025-01-26"
---

# 📋 13.4 策略开发模块

> **核心摘要：**
> 
> 本节介绍韬睿量化系统的策略开发流程，包括策略模板、参数配置、信号生成、组合构建和版本管理。通过标准化的策略框架，提升策略开发效率和代码质量。

## 📋 章节概览

<div class="section-overview">
  <div class="overview-item">
    <span class="item-icon">📋</span>
    <a href="#template">策略模板</a>
    <span class="item-desc">标准化策略框架</span>
  </div>
  <div class="overview-item">
    <span class="item-icon">⚙️</span>
    <a href="#config">参数配置</a>
    <span class="item-desc">YAML配置管理</span>
  </div>
  <div class="overview-item">
    <span class="item-icon">🎯</span>
    <a href="#signal">信号生成</a>
    <span class="item-desc">交易信号逻辑</span>
  </div>
  <div class="overview-item">
    <span class="item-icon">📊</span>
    <a href="#portfolio">组合构建</a>
    <span class="item-desc">选股和权重分配</span>
  </div>
</div>

---

<h2 id="template">📋 策略模板</h2>

### 策略基类设计

<div class="board-details">
  <div class="board-card">
    <h4>📋 策略基类（BaseStrategy）</h4>
    <div class="board-content">

```python
# strategies/base_strategy.py
from abc import ABC, abstractmethod
import pandas as pd
import numpy as np
from datetime import datetime

class BaseStrategy(ABC):
    """
    策略基类
    所有策略必须继承此类并实现抽象方法
    """
    
    def __init__(self, config: dict):
        """
        初始化策略
        
        参数:
            config: 策略配置字典
        """
        self.config = config
        self.name = config.get('name', 'Unnamed Strategy')
        self.universe = config.get('universe', 'hs300')
        self.hold_num = config.get('hold_num', 30)
        self.rebalance_freq = config.get('rebalance_freq', 'monthly')
        
        # 数据提供器
        self.data_provider = None
        
        # 因子计算器
        self.factor_calculator = None
        
        # 日志
        self.logger = self._setup_logger()
    
    @abstractmethod
    def generate_signals(self, date: datetime) -> list:
        """
        生成交易信号（子类必须实现）
        
        参数:
            date: 信号日期
        
        返回:
            目标股票列表
        """
        pass
    
    @abstractmethod
    def calculate_weights(self, stocks: list, date: datetime) -> dict:
        """
        计算持仓权重（子类必须实现）
        
        参数:
            stocks: 股票列表
            date: 计算日期
        
        返回:
            {股票代码: 权重}字典
        """
        pass
    
    def get_universe(self, date: datetime) -> list:
        """获取股票池"""
        if self.universe == 'hs300':
            return get_index_stocks('000300.XSHG', date)
        elif self.universe == 'zz500':
            return get_index_stocks('000905.XSHG', date)
        elif self.universe == 'all':
            return get_all_securities(date=date).index.tolist()
        else:
            return self.universe  # 自定义股票池
    
    def filter_stocks(self, stocks: list, date: datetime) -> list:
        """过滤股票（排除ST、停牌等）"""
        # 排除ST
        st_stocks = get_extras('is_st', stocks, end_date=date)
        st_stocks = st_stocks.iloc[-1]
        stocks = [s for s in stocks if not st_stocks.get(s, True)]
        
        # 排除停牌
        paused = get_price(stocks, end_date=date, count=1, 
                          fields=['paused'])['paused'].iloc[-1]
        stocks = [s for s in stocks if not paused.get(s, True)]
        
        # 排除上市不足60天
        securities = get_all_securities()
        stocks = [s for s in stocks 
                  if (date - securities.loc[s, 'start_date']).days > 60]
        
        return stocks
    
    def zscore(self, series: pd.Series) -> pd.Series:
        """Z-Score标准化"""
        return (series - series.mean()) / series.std()
    
    def winsorize(self, series: pd.Series, n_std: float = 3) -> pd.Series:
        """去极值（MAD方法）"""
        median = series.median()
        mad = (series - median).abs().median()
        upper = median + n_std * 1.4826 * mad
        lower = median - n_std * 1.4826 * mad
        return series.clip(lower, upper)
```

</div>
  </div>
</div>

### 多因子策略模板

<div class="board-details">
  <div class="board-card">
    <h4>📋 多因子策略模板</h4>
    <div class="board-content">

```python
# strategies/multi_factor_strategy.py
from strategies.base_strategy import BaseStrategy
import pandas as pd

class MultiFactorStrategy(BaseStrategy):
    """
    多因子选股策略
    
    支持的因子：
    - ep: 市盈率倒数
    - bp: 市净率倒数
    - roe: 净资产收益率
    - growth: 营收增速
    - momentum: 价格动量
    - quality: 毛利率
    - north_flow: 北向资金
    """
    
    def __init__(self, config: dict):
        super().__init__(config)
        
        # 因子配置
        self.factors = config.get('factors', ['ep', 'roe', 'momentum'])
        self.weights = config.get('weights', None)
        if self.weights is None:
            self.weights = [1/len(self.factors)] * len(self.factors)
        
        # 行业中性化
        self.industry_neutral = config.get('industry_neutral', True)
        
        # 市值中性化
        self.size_neutral = config.get('size_neutral', False)
    
    def generate_signals(self, date):
        """生成多因子选股信号"""
        # 1. 获取股票池
        universe = self.get_universe(date)
        stocks = self.filter_stocks(universe, date)
        
        self.logger.info(f"股票池: {len(stocks)}只")
        
        # 2. 计算各因子
        factor_scores = pd.DataFrame({'code': stocks})
        
        for i, factor in enumerate(self.factors):
            factor_values = self._calc_factor(factor, stocks, date)
            
            # 预处理
            factor_values = self.winsorize(factor_values)
            factor_values = self.zscore(factor_values)
            
            # 行业中性化
            if self.industry_neutral:
                factor_values = self._neutralize_industry(
                    factor_values, stocks, date
                )
            
            factor_scores[factor] = factor_values * self.weights[i]
        
        # 3. 计算综合得分
        factor_cols = [f for f in self.factors]
        factor_scores['total_score'] = factor_scores[factor_cols].sum(axis=1)
        
        # 4. 选择Top N
        factor_scores = factor_scores.sort_values('total_score', ascending=False)
        target_stocks = factor_scores.head(self.hold_num)['code'].tolist()
        
        self.logger.info(f"选出: {len(target_stocks)}只股票")
        
        return target_stocks
    
    def calculate_weights(self, stocks, date):
        """计算持仓权重（等权）"""
        weight = 1.0 / len(stocks)
        return {stock: weight for stock in stocks}
    
    def _calc_factor(self, factor_name, stocks, date):
        """计算单个因子"""
        if factor_name == 'ep':
            return self._calc_ep(stocks, date)
        elif factor_name == 'roe':
            return self._calc_roe(stocks, date)
        elif factor_name == 'momentum':
            return self._calc_momentum(stocks, date)
        elif factor_name == 'growth':
            return self._calc_growth(stocks, date)
        elif factor_name == 'quality':
            return self._calc_quality(stocks, date)
        elif factor_name == 'north_flow':
            return self._calc_north_flow(stocks, date)
        else:
            raise ValueError(f"Unknown factor: {factor_name}")
    
    def _calc_ep(self, stocks, date):
        """计算EP因子"""
        df = get_fundamentals(query(
            valuation.code, valuation.pe_ratio
        ).filter(
            valuation.code.in_(stocks),
            valuation.pe_ratio > 0
        ), date=date)
        return 1 / df.set_index('code')['pe_ratio']
    
    def _calc_roe(self, stocks, date):
        """计算ROE因子"""
        df = get_fundamentals(query(
            valuation.code, indicator.roe
        ).filter(valuation.code.in_(stocks)), date=date)
        return df.set_index('code')['roe']
    
    def _calc_momentum(self, stocks, date, window=20):
        """计算动量因子"""
        prices = get_price(stocks, end_date=date, count=window+1, 
                          fields=['close'])
        returns = prices['close'].iloc[-1] / prices['close'].iloc[0] - 1
        return returns
    
    def _calc_growth(self, stocks, date):
        """计算成长因子"""
        df = get_fundamentals(query(
            valuation.code, indicator.inc_revenue_year_on_year
        ).filter(valuation.code.in_(stocks)), date=date)
        return df.set_index('code')['inc_revenue_year_on_year']
    
    def _calc_quality(self, stocks, date):
        """计算质量因子"""
        df = get_fundamentals(query(
            valuation.code, indicator.gross_profit_margin
        ).filter(valuation.code.in_(stocks)), date=date)
        return df.set_index('code')['gross_profit_margin']
    
    def _neutralize_industry(self, factor_values, stocks, date):
        """行业中性化"""
        industries = get_industry(stocks, date)
        df = pd.DataFrame({
            'factor': factor_values,
            'industry': [industries.get(s, 'Unknown') for s in stocks]
        }, index=stocks)
        
        # 按行业去均值
        df['factor_neutral'] = df.groupby('industry')['factor'].transform(
            lambda x: x - x.mean()
        )
        return df['factor_neutral']
```

</div>
  </div>
</div>

---

<h2 id="config">⚙️ 参数配置</h2>

### YAML配置文件

<div class="board-details">
  <div class="board-card">
    <h4>⚙️ 策略配置文件</h4>
    <div class="board-content">

```python
# config/strategies/multi_factor_hs300.yaml

# 策略基本信息
strategy:
  name: "沪深300多因子增强"
  type: "multi_factor"
  version: "1.0.0"
  author: "韬睿量化"
  description: "基于价值、成长、质量、动量四因子的沪深300增强策略"

# 股票池配置
universe:
  type: "index"
  code: "000300.XSHG"
  name: "沪深300"

# 因子配置
factors:
  - name: "ep"
    weight: 0.30
    direction: 1  # 1=越大越好，-1=越小越好
    description: "市盈率倒数"
  
  - name: "roe"
    weight: 0.25
    direction: 1
    description: "净资产收益率"
  
  - name: "growth"
    weight: 0.25
    direction: 1
    description: "营收增速"
  
  - name: "momentum"
    weight: 0.20
    direction: 1
    description: "20日动量"

# 因子处理配置
preprocessing:
  winsorize: true
  winsorize_std: 3
  standardize: true
  industry_neutral: true
  size_neutral: false

# 交易配置
trading:
  hold_num: 30
  rebalance_freq: "monthly"  # monthly, weekly, daily
  rebalance_day: 1  # 每月第几个交易日
  
# 风险控制
risk:
  max_weight: 0.05  # 单只股票最大权重
  min_weight: 0.01  # 单只股票最小权重
  max_industry_weight: 0.30  # 单一行业最大权重
  max_turnover: 0.50  # 单次调仓最大换手率
  stop_loss: -0.10  # 个股止损线

# 交易成本
cost:
  commission: 0.0003  # 佣金率
  slippage: 0.002  # 滑点
  stamp_duty: 0.001  # 印花税（卖出）

# 基准
benchmark:
  code: "000300.XSHG"
  name: "沪深300"
```

</div>
  </div>
</div>

### 配置加载器

<div class="board-details">
  <div class="board-card">
    <h4>📋 配置加载代码</h4>
    <div class="board-content">

```python
# config/config_loader.py
import yaml
from pathlib import Path

class ConfigLoader:
    """配置加载器"""
    
    def __init__(self, config_dir: str = 'config/strategies'):
        self.config_dir = Path(config_dir)
    
    def load(self, strategy_name: str) -> dict:
        """加载策略配置"""
        config_path = self.config_dir / f"{strategy_name}.yaml"
        
        if not config_path.exists():
            raise FileNotFoundError(f"Config not found: {config_path}")
        
        with open(config_path, 'r', encoding='utf-8') as f:
            config = yaml.safe_load(f)
        
        # 验证配置
        self._validate(config)
        
        return config
    
    def _validate(self, config: dict):
        """验证配置完整性"""
        required_keys = ['strategy', 'universe', 'factors', 'trading']
        for key in required_keys:
            if key not in config:
                raise ValueError(f"Missing required config: {key}")
        
        # 验证因子权重和为1
        weights = [f['weight'] for f in config['factors']]
        if abs(sum(weights) - 1.0) > 0.01:
            raise ValueError(f"Factor weights must sum to 1, got {sum(weights)}")

# 使用示例
loader = ConfigLoader()
config = loader.load('multi_factor_hs300')
```

</div>
  </div>
</div>

---

<h2 id="signal">🎯 信号生成</h2>

### 信号生成流程

<div class="exchange-grid">
  <div class="exchange-card">
    <div class="exchange-header">
      <span class="exchange-icon">1️⃣</span>
      <h3>获取股票池</h3>
    </div>
    <div class="exchange-content">
      <p><strong>指数成分股：</strong>沪深300、中证500等</p>
      <p><strong>全市场：</strong>全A股票</p>
      <p><strong>自定义：</strong>行业、主题股票池</p>
    </div>
  </div>

  <div class="exchange-card">
    <div class="exchange-header">
      <span class="exchange-icon">2️⃣</span>
      <h3>股票过滤</h3>
    </div>
    <div class="exchange-content">
      <p><strong>排除ST：</strong>ST、*ST股票</p>
      <p><strong>排除停牌：</strong>当日停牌股票</p>
      <p><strong>排除新股：</strong>上市不足60天</p>
    </div>
  </div>

  <div class="exchange-card">
    <div class="exchange-header">
      <span class="exchange-icon">3️⃣</span>
      <h3>因子计算</h3>
    </div>
    <div class="exchange-content">
      <p><strong>计算因子值：</strong>各因子原始值</p>
      <p><strong>预处理：</strong>去极值、标准化</p>
      <p><strong>中性化：</strong>行业、市值中性化</p>
    </div>
  </div>

  <div class="exchange-card">
    <div class="exchange-header">
      <span class="exchange-icon">4️⃣</span>
      <h3>综合评分</h3>
    </div>
    <div class="exchange-content">
      <p><strong>加权合成：</strong>按权重合成综合得分</p>
      <p><strong>排序选股：</strong>选择得分最高的N只</p>
      <p><strong>输出信号：</strong>目标股票列表</p>
    </div>
  </div>
</div>

---

<h2 id="portfolio">📊 组合构建</h2>

### 权重分配方法

<div class="comparison-table">
  <table>
    <thead>
      <tr>
        <th>方法</th>
        <th>原理</th>
        <th>优点</th>
        <th>缺点</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>等权</strong></td>
        <td>每只股票相同权重</td>
        <td>简单、分散</td>
        <td>不考虑因子强度</td>
      </tr>
      <tr>
        <td><strong>因子加权</strong></td>
        <td>按因子得分分配</td>
        <td>强调高分股票</td>
        <td>集中度可能过高</td>
      </tr>
      <tr>
        <td><strong>市值加权</strong></td>
        <td>按市值分配</td>
        <td>流动性好</td>
        <td>大市值偏重</td>
      </tr>
      <tr>
        <td><strong>风险平价</strong></td>
        <td>按风险贡献分配</td>
        <td>风险均衡</td>
        <td>计算复杂</td>
      </tr>
      <tr>
        <td><strong>优化</strong></td>
        <td>均值方差优化</td>
        <td>理论最优</td>
        <td>对输入敏感</td>
      </tr>
    </tbody>
  </table>
</div>

## 💎 核心要点总结

<div class="key-points">
  <div class="key-point">
    <h4>📋 标准化框架</h4>
    <p>策略基类提供统一接口，子类实现具体逻辑</p>
  </div>
  <div class="key-point">
    <h4>⚙️ 配置化管理</h4>
    <p>YAML配置文件管理策略参数，无需修改代码</p>
  </div>
  <div class="key-point">
    <h4>🎯 模块化设计</h4>
    <p>因子计算、预处理、选股、权重各模块独立</p>
  </div>
  <div class="key-point">
    <h4>📊 可扩展性强</h4>
    <p>易于添加新因子、新策略类型</p>
  </div>
</div>

## 🔮 总结与展望

<div class="summary-outlook">
  <h3>本节回顾</h3>
  <p>本节介绍了韬睿量化系统的策略开发框架，包括策略基类设计、多因子策略模板、YAML配置管理、信号生成流程和组合构建方法。</p>
  
  <h3>下节预告</h3>
  <p>下一节将学习回测与实盘模块的使用方法，包括回测引擎和PTrade/QMT对接。</p>
  
  <a href="/ashare-book4/013_Chapter13/13.5_Backtest_Live_CN" class="next-section">
    继续学习：13.5 回测与实盘模块 →
  </a>
</div>
