---
/**
 * 实时新闻中心 - 简化版
 * 使用本地数据 + 模拟实时效果
 */
import Layout from '../layouts/Layout.astro';
---

<Layout title="实时新闻中心">
  <style>
    /* 全局样式 */
    :root {
      --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      --success-gradient: linear-gradient(135deg, #0ba360 0%, #3cba92 100%);
      --warning-gradient: linear-gradient(135deg, #f2994a 0%, #f2c94c 100%);
      --info-gradient: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      color: #2d3748;
    }

    /* 主容器 */
    .news-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 20px;
    }

    /* 页面头部 */
    .page-header {
      background: white;
      border-radius: 20px;
      padding: 40px;
      margin-bottom: 30px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
      text-align: center;
    }

    .page-header h1 {
      font-size: 3em;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 15px;
    }

    .page-subtitle {
      font-size: 1.2em;
      color: #718096;
      margin-bottom: 20px;
    }

    /* 控制面板 */
    .control-panel {
      display: flex;
      gap: 15px;
      justify-content: center;
      flex-wrap: wrap;
      margin-top: 25px;
    }

    .control-btn {
      padding: 12px 30px;
      border: none;
      border-radius: 10px;
      font-size: 1em;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 8px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .control-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
    }

    .btn-refresh {
      background: var(--success-gradient);
      color: white;
    }

    .btn-auto-refresh {
      background: var(--info-gradient);
      color: white;
    }

    .btn-category {
      background: white;
      color: #667eea;
      border: 2px solid #667eea;
    }

    .btn-category.active {
      background: var(--primary-gradient);
      color: white;
      border-color: transparent;
    }

    /* 统计面板 */
    .stats-panel {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }

    .stat-card {
      background: white;
      border-radius: 15px;
      padding: 25px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      display: flex;
      align-items: center;
      gap: 20px;
      transition: transform 0.3s ease;
    }

    .stat-card:hover {
      transform: translateY(-5px);
    }

    .stat-icon {
      font-size: 3em;
      width: 70px;
      height: 70px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 15px;
      background: var(--primary-gradient);
    }

    .stat-content h3 {
      font-size: 0.9em;
      color: #718096;
      margin-bottom: 8px;
    }

    .stat-number {
      font-size: 2em;
      font-weight: bold;
      color: #2d3748;
    }

    /* 新闻网格 */
    .news-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(400px, 1fr));
      gap: 25px;
      margin-bottom: 30px;
    }

    /* 新闻卡片 */
    .news-card {
      background: white;
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
      display: flex;
      flex-direction: column;
    }

    .news-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
    }

    .news-image {
      width: 100%;
      height: 220px;
      object-fit: cover;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .news-content {
      padding: 25px;
      flex-grow: 1;
      display: flex;
      flex-direction: column;
    }

    .news-meta {
      display: flex;
      gap: 10px;
      margin-bottom: 15px;
      flex-wrap: wrap;
    }

    .news-tag {
      padding: 5px 12px;
      border-radius: 20px;
      font-size: 0.85em;
      font-weight: 600;
    }

    .tag-source {
      background: #e6f3ff;
      color: #0066cc;
    }

    .tag-category {
      background: #f0f4ff;
      color: #667eea;
    }

    .news-title {
      font-size: 1.3em;
      font-weight: 700;
      color: #2d3748;
      margin-bottom: 12px;
      line-height: 1.4;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }

    .news-summary {
      font-size: 0.95em;
      color: #718096;
      line-height: 1.6;
      margin-bottom: 15px;
      display: -webkit-box;
      -webkit-line-clamp: 3;
      -webkit-box-orient: vertical;
      overflow: hidden;
      flex-grow: 1;
    }

    .news-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-top: 15px;
      border-top: 1px solid #e2e8f0;
    }

    .news-time {
      font-size: 0.9em;
      color: #a0aec0;
    }

    .news-link {
      padding: 8px 20px;
      background: var(--primary-gradient);
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-weight: 600;
      font-size: 0.9em;
      transition: all 0.3s ease;
    }

    .news-link:hover {
      transform: scale(1.05);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }

    /* 响应式设计 */
    @media (max-width: 768px) {
      .page-header h1 {
        font-size: 2em;
      }

      .news-grid {
        grid-template-columns: 1fr;
      }

      .control-panel {
        flex-direction: column;
      }

      .control-btn {
        width: 100%;
        justify-content: center;
      }
    }
  </style>

  <div class="news-container">
    <!-- 页面头部 -->
    <header class="page-header">
      <h1>📰 实时新闻中心</h1>
      <p class="page-subtitle">多源聚合 · 智能分析 · 投资洞察</p>
      
      <!-- 控制面板 -->
      <div class="control-panel">
        <button class="control-btn btn-refresh" id="btnRefresh">
          <span>🔄</span>
          <span>刷新新闻</span>
        </button>
        <button class="control-btn btn-auto-refresh" id="btnAutoRefresh">
          <span>⏰</span>
          <span>自动刷新: 关闭</span>
        </button>
        <button class="control-btn btn-category active" data-category="stocks">
          📈 股票
        </button>
        <button class="control-btn btn-category" data-category="tech">
          💻 科技
        </button>
        <button class="control-btn btn-category" data-category="business">
          💼 商业
        </button>
        <button class="control-btn btn-category" data-category="economy">
          💰 经济
        </button>
      </div>
    </header>

    <!-- 统计面板 -->
    <div class="stats-panel">
      <div class="stat-card">
        <div class="stat-icon">📰</div>
        <div class="stat-content">
          <h3>新闻总数</h3>
          <div class="stat-number" id="totalNews">0</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon">🔄</div>
        <div class="stat-content">
          <h3>最后更新</h3>
          <div class="stat-number" id="lastUpdate" style="font-size: 1.2em;">--:--</div>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon">📡</div>
        <div class="stat-content">
          <h3>当前类别</h3>
          <div class="stat-number" id="currentCategory" style="font-size: 1.3em;">股票</div>
        </div>
      </div>
    </div>

    <!-- 新闻网格 -->
    <div class="news-grid" id="newsGrid"></div>
  </div>

  <script>
    // 模拟新闻数据
    const mockNews = {
      stocks: [
        {
          title: "美股三大指数收涨，科技股领涨",
          summary: "美国股市周一收高，科技股表现强劲，投资者对经济复苏持乐观态度。纳斯达克指数上涨1.2%，标普500指数上涨0.8%。",
          source: "Bloomberg",
          time: "2小时前",
          url: "#"
        },
        {
          title: "苹果公司发布最新财报，营收超预期",
          summary: "苹果公司公布第四季度财报，营收达到创纪录的1239亿美元，同比增长8%，超出分析师预期。",
          source: "Reuters",
          time: "4小时前",
          url: "#"
        },
        {
          title: "特斯拉股价大涨，市值突破8000亿美元",
          summary: "特斯拉股价周一上涨12%，市值突破8000亿美元，创历史新高。投资者看好其自动驾驶技术发展。",
          source: "CNBC",
          time: "6小时前",
          url: "#"
        }
      ],
      tech: [
        {
          title: "OpenAI发布GPT-5，AI能力再升级",
          summary: "OpenAI正式发布GPT-5，新模型在推理能力和多模态理解方面有显著提升，预计将推动AI应用进一步发展。",
          source: "TechCrunch",
          time: "1小时前",
          url: "#"
        },
        {
          title: "微软Azure云服务收入增长强劲",
          summary: "微软公布最新财报，Azure云服务收入同比增长35%，企业数字化转型需求持续增长。",
          source: "ZDNet",
          time: "3小时前",
          url: "#"
        }
      ],
      business: [
        {
          title: "全球供应链逐步恢复，制造业PMI回升",
          summary: "最新数据显示，全球制造业PMI指数连续三个月回升，供应链瓶颈问题得到缓解，经济复苏迹象明显。",
          source: "WSJ",
          time: "5小时前",
          url: "#"
        }
      ],
      economy: [
        {
          title: "美联储维持利率不变，通胀压力缓解",
          summary: "美联储宣布维持基准利率在5.25%-5.5%区间不变，通胀数据显示压力有所缓解，市场反应积极。",
          source: "Federal Reserve",
          time: "7小时前",
          url: "#"
        }
      ]
    };

    // 全局状态
    let currentCategory = 'stocks';
    let autoRefreshInterval = null;
    let isAutoRefreshEnabled = false;

    // DOM元素
    const newsGrid = document.getElementById('newsGrid');
    const totalNews = document.getElementById('totalNews');
    const lastUpdate = document.getElementById('lastUpdate');
    const currentCategoryEl = document.getElementById('currentCategory');
    const btnRefresh = document.getElementById('btnRefresh');
    const btnAutoRefresh = document.getElementById('btnAutoRefresh');
    const categoryBtns = document.querySelectorAll('.btn-category');

    // 类别映射
    const categoryNames = {
      'stocks': '股票',
      'tech': '科技',
      'business': '商业',
      'economy': '经济'
    };

    // 显示新闻
    function displayNews(category) {
      const news = mockNews[category] || [];
      newsGrid.innerHTML = '';
      
      news.forEach(item => {
        const card = createNewsCard(item);
        newsGrid.appendChild(card);
      });

      updateStats(news.length);
    }

    // 创建新闻卡片
    function createNewsCard(item) {
      const card = document.createElement('div');
      card.className = 'news-card';
      
      card.innerHTML = `
        <div class="news-image"></div>
        <div class="news-content">
          <div class="news-meta">
            <span class="news-tag tag-source">${item.source}</span>
            <span class="news-tag tag-category">${categoryNames[currentCategory]}</span>
          </div>
          <h3 class="news-title">${item.title}</h3>
          <p class="news-summary">${item.summary}</p>
          <div class="news-footer">
            <span class="news-time">🕐 ${item.time}</span>
            <a href="${item.url}" target="_blank" rel="noopener noreferrer" class="news-link">
              阅读全文
            </a>
          </div>
        </div>
      `;
      
      return card;
    }

    // 更新统计信息
    function updateStats(count) {
      totalNews.textContent = count;
      const now = new Date();
      lastUpdate.textContent = now.toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' });
      currentCategoryEl.textContent = categoryNames[currentCategory];
    }

    // 模拟刷新
    function refreshNews() {
      // 添加一些随机性
      const news = mockNews[currentCategory];
      if (news && news.length > 0) {
        // 随机打乱新闻顺序
        const shuffled = [...news].sort(() => Math.random() - 0.5);
        mockNews[currentCategory] = shuffled;
        displayNews(currentCategory);
      }
    }

    // 切换自动刷新
    function toggleAutoRefresh() {
      isAutoRefreshEnabled = !isAutoRefreshEnabled;
      
      if (isAutoRefreshEnabled) {
        btnAutoRefresh.querySelector('span:last-child').textContent = '自动刷新: 开启';
        
        // 每30秒刷新一次（演示用）
        autoRefreshInterval = setInterval(() => {
          console.log('自动刷新新闻...');
          refreshNews();
        }, 30000);
      } else {
        btnAutoRefresh.querySelector('span:last-child').textContent = '自动刷新: 关闭';
        
        if (autoRefreshInterval) {
          clearInterval(autoRefreshInterval);
          autoRefreshInterval = null;
        }
      }
    }

    // 事件监听
    btnRefresh.addEventListener('click', refreshNews);
    btnAutoRefresh.addEventListener('click', toggleAutoRefresh);

    categoryBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        // 移除所有active类
        categoryBtns.forEach(b => b.classList.remove('active'));
        // 添加当前active类
        btn.classList.add('active');
        
        // 更新类别并显示新闻
        currentCategory = btn.dataset.category;
        displayNews(currentCategory);
      });
    });

    // 页面加载时显示新闻
    displayNews('stocks');
  </script>
</Layout>










