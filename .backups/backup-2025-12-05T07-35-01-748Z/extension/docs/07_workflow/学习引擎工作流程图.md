# 学习引擎工作流程图

> **TRQuant 策略优化器 - 学习引擎详细流程**

---

## 📊 总体架构流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                    TRQuant 学习引擎架构                          │
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │  学习引擎    │
                    │  核心服务    │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼─────┐      ┌────▼─────┐      ┌────▼─────┐
   │文档学习器 │      │历史学习器 │      │手册学习器 │
   │Document  │      │History   │      │Manual    │
   │Learner   │      │Learner   │      │Learner   │
   └────┬─────┘      └────┬─────┘      └────┬─────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    ┌──────▼───────┐
                    │  知识存储库  │
                    │KnowledgeStore│
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  智能推荐    │
                    │Recommendation│
                    └──────────────┘
```

---

## 🔄 完整学习流程（详细版）

### 阶段1: 初始化

```
[启动]
  ↓
检查存储路径
  ├── 存在 → 加载已有知识库
  └── 不存在 → 创建默认知识库
  ↓
初始化学习器
  ├── StrategyLearner
  ├── ManualLearner
  └── KnowledgeStore
  ↓
[就绪]
```

### 阶段2: 多源学习

#### 2.1 文档学习流程

```
输入: 文档路径列表
  ↓
┌─────────────────────────────────────┐
│ 步骤1: 文件扫描                      │
│  ├── 遍历文档目录                    │
│  ├── 过滤 .md, .py, .txt 文件       │
│  └── 读取文件内容                    │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤2: 内容解析                      │
│  ├── 提取代码块 (```python ... ```) │
│  ├── 提取函数定义 (def function)    │
│  ├── 提取最佳实践标记                │
│  └── 提取风控规则                    │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤3: 模式识别                      │
│  ├── 选股模式 (select/filter/stock) │
│  ├── 择时模式 (timing/signal)       │
│  ├── 风控模式 (stop/risk/position)  │
│  └── 执行模式 (order/trade)         │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤4: 知识提取                      │
│  ├── 创建 StrategyPattern           │
│  ├── 创建 BestPractice              │
│  └── 更新 KnowledgeStore            │
└──────────────┬──────────────────────┘
               ↓
输出: {learned, patterns, practices}
```

#### 2.2 历史学习流程

```
输入: 历史策略列表
  ↓
┌─────────────────────────────────────┐
│ 步骤1: 策略分析                      │
│  ├── CodeAnalyzer.analyze()         │
│  ├── 提取组件 (选股/择时/风控)      │
│  └── 识别因子和指标                 │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤2: 表现评估                      │
│  ├── 成功策略:                      │
│  │   returns > 10%                  │
│  │   sharpe > 1.0                   │
│  │   drawdown < -20%                │
│  └── 失败策略:                      │
│      returns < -10%                  │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤3: 有效性更新                    │
│  ├── 成功策略 → 模式有效性 +5       │
│  ├── 失败策略 → 模式有效性 -3       │
│  └── 有风控 → 风控模式有效性 +2     │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤4: 经验总结                      │
│  ├── 记录成功模式组合                │
│  ├── 记录失败原因                    │
│  └── 更新最佳实践                    │
└──────────────┬──────────────────────┘
               ↓
输出: {analyzed, successPatterns, failurePatterns}
```

#### 2.3 手册学习流程（实时更新）

```
[初始化]
  ↓
扫描 AShare-manual/src/pages/
  ├── ashare-book1/ (A股投资基础)
  ├── ashare-book2/ (宏观经济)
  ├── ashare-book3/ (个股研究)
  ├── ashare-book4/ (技术分析)
  ├── ashare-book5/ (量化专题)
  └── ashare/ (A股量化核心)
  ↓
建立章节映射
  ↓
┌─────────────────────────────────────┐
│ 批量学习阶段                         │
│  ├── 遍历所有 .md 文件              │
│  ├── 提取代码块和知识                │
│  └── 分类存储到知识库                │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 实时监听阶段                         │
│  ├── fs.watch(pages目录)            │
│  ├── 检测 .md 文件变化              │
│  └── 自动重新学习变更文件            │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 增量更新阶段                         │
│  ├── 提取新内容                      │
│  ├── 更新知识库                      │
│  └── 通知用户更新完成                │
└──────────────┬──────────────────────┘
               ↓
输出: 实时同步的知识库
```

#### 2.4 反馈学习流程

```
输入: 用户反馈
  ├── correction (纠正)
  ├── improvement (改进)
  ├── bug (问题)
  └── feature (功能)
  ↓
┌─────────────────────────────────────┐
│ 步骤1: 反馈分类                      │
│  ├── 纠正 → 学习API映射             │
│  ├── 改进 → 创建最佳实践             │
│  ├── 问题 → 记录待修复               │
│  └── 功能 → 记录需求                 │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤2: 知识提取                      │
│  ├── 从纠正中提取新的API映射规则     │
│  ├── 从改进中提取最佳实践            │
│  └── 更新知识库                      │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤3: 反馈处理                      │
│  ├── 标记为已处理                    │
│  └── 保存到知识库                    │
└──────────────┬──────────────────────┘
               ↓
输出: 改进的知识库
```

### 阶段3: 知识存储

```
学习结果
  ↓
┌─────────────────────────────────────┐
│ KnowledgeStore 存储                  │
│  ├── patterns[]                     │
│  │   ├── id                         │
│  │   ├── name                       │
│  │   ├── category                   │
│  │   ├── codeTemplate               │
│  │   ├── effectiveness              │
│  │   └── usageCount                 │
│  ├── bestPractices[]                │
│  │   ├── id                         │
│  │   ├── title                      │
│  │   ├── category                   │
│  │   ├── description                │
│  │   └── example                    │
│  ├── apiMappings[]                  │
│  │   ├── source                     │
│  │   ├── target                     │
│  │   └── paramMapping               │
│  └── userFeedback[]                 │
│      ├── timestamp                  │
│      ├── type                       │
│      └── content                     │
└──────────────┬──────────────────────┘
               ↓
持久化到 knowledge.json
```

### 阶段4: 智能推荐

```
用户输入策略代码
  ↓
┌─────────────────────────────────────┐
│ 步骤1: 代码分析                      │
│  ├── 分析代码结构                    │
│  ├── 识别已有组件                    │
│  └── 检测缺失功能                    │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤2: 知识匹配                      │
│  ├── 查询知识库中的模式              │
│  ├── 查询最佳实践                    │
│  └── 匹配相关API映射                 │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤3: 推荐生成                      │
│  ├── 如果缺少风控                    │
│  │   → 推荐风控模式                  │
│  ├── 如果选股简单                    │
│  │   → 推荐复杂选股模式              │
│  ├── 如果缺少止损                    │
│  │   → 推荐止损最佳实践              │
│  └── 如果缺少日志                    │
│       → 推荐日志最佳实践             │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ 步骤4: 排序和过滤                    │
│  ├── 按有效性排序                    │
│  ├── 去重                            │
│  └── 返回TOP推荐                     │
└──────────────┬──────────────────────┘
               ↓
输出: 推荐列表 (patterns, practices)
```

---

## 🔁 实时更新流程

```
文件系统变化
  ↓
fs.watch 触发
  ↓
检测文件类型
  ├── .md 文件 → 继续处理
  └── 其他文件 → 忽略
  ↓
读取文件内容
  ↓
提取知识
  ├── 代码块
  ├── 最佳实践
  └── 风控规则
  ↓
更新知识库
  ├── 添加新模式
  ├── 更新已有模式
  └── 保存到文件
  ↓
通知用户
  └── "手册更新已学习"
```

---

## 📈 数据流转图

### 学习数据流

```
┌─────────────┐
│ 学习源      │
│             │
│ • 文档      │
│ • 历史策略  │
│ • 实操手册  │
│ • 用户反馈  │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 学习器      │
│             │
│ • 解析      │
│ • 提取      │
│ • 识别      │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 知识存储库  │
│             │
│ • 模式      │
│ • 实践      │
│ • 映射      │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 智能推荐    │
│             │
│ • 匹配      │
│ • 排序      │
│ • 过滤      │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 策略优化    │
│             │
│ • 应用推荐  │
│ • 生成报告  │
└─────────────┘
```

### 推荐数据流

```
策略代码
  ↓
代码分析
  ├── 组件识别
  ├── API检测
  └── 因子识别
  ↓
知识查询
  ├── 查询相关模式
  ├── 查询最佳实践
  └── 查询API映射
  ↓
推荐生成
  ├── 缺失功能检测
  ├── 相关性计算
  └── 有效性排序
  ↓
推荐结果
  ├── 策略模式推荐
  ├── 最佳实践推荐
  └── API转换建议
```

---

## 🎯 关键决策点

### 决策点1: 模式有效性更新

```
策略表现评估
  ↓
┌─────────────┐
│ 成功策略?   │
└───┬─────┬───┘
    │     │
  是│     │否
    ↓     ↓
有效性+5  有效性-3
    │     │
    └───┬─┘
        ↓
  更新知识库
```

### 决策点2: 推荐优先级

```
代码分析结果
  ↓
┌─────────────┐
│ 缺少风控?   │
└───┬─────┬───┘
    │     │
  是│     │否
    ↓     ↓
推荐风控  检查其他
    │     │
    └───┬─┘
        ↓
  最终推荐列表
```

---

## 📊 性能优化流程

```
学习请求
  ↓
检查缓存
  ├── 命中 → 返回缓存结果
  └── 未命中 → 继续处理
  ↓
异步处理
  ├── 文件I/O异步
  ├── 解析异步
  └── 存储异步
  ↓
批量处理
  ├── 批量读取文件
  ├── 批量解析
  └── 批量存储
  ↓
结果缓存
  └── 保存到内存缓存
```

---

## 🔍 错误处理流程

```
学习过程
  ↓
┌─────────────┐
│ 发生错误?   │
└───┬─────┬───┘
    │     │
  是│     │否
    ↓     ↓
错误分类  继续处理
  ├── 文件不存在
  ├── 解析失败
  └── 存储失败
    ↓
错误处理
  ├── 记录日志
  ├── 跳过当前项
  └── 继续处理下一项
    ↓
返回结果
  └── 包含错误信息
```

---

## 📝 总结

### 学习引擎核心特点

1. **多源学习**: 从文档、历史、手册、反馈四个维度学习
2. **实时更新**: 支持文件监听，自动同步最新内容
3. **智能推荐**: 基于代码分析，智能推荐相关模式和实践
4. **持续进化**: 知识库不断积累，有效性动态调整

### 工作流程关键步骤

1. **初始化** → 加载/创建知识库
2. **学习** → 多源数据提取和解析
3. **存储** → 持久化到知识库
4. **推荐** → 基于分析结果智能推荐
5. **优化** → 应用推荐优化策略

---

**文档版本**: 1.0.0  
**最后更新**: 2025-12-03

